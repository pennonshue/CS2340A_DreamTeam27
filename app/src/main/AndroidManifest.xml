<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DungeonGame"
        tools:targetApi="31">
        <activity
            android:name=".GameScreen"
            android:exported="false"
            android:label="GameScreen"
            android:theme="@style/Theme.DungeonGame" />
        <activity
            android:name=".GameScreen2"
            android:exported="false"
            android:label="GameScreen2"
            android:theme="@style/Theme.DungeonGame" />
        <activity
            android:name=".GameScreen3"
            android:exported="false"
            android:label="GameScreen3"
            android:theme="@style/Theme.DungeonGame" />

        <activity
            android:name=".EndScreen"
            android:exported="false"
            android:label="EndScreen"
            android:theme="@style/Theme.DungeonGame" />
        <activity
            android:name=".SetupActivity"
            android:exported="false"
            android:label="@string/title_activity_setup"
            android:theme="@style/Theme.DungeonGame" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>